apiVersion: v1
kind: Service

metadata:
  name: {{ .Release.Name }}-headless

spec:
  clusterIP: None

  selector:
    app: {{ .Release.Name }}

  ports:
    - name: api
      port: 8200
      targetPort: 8200

    - name: cluster
      port: 8201
      targetPort: 8201