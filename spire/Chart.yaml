apiVersion: v2
name: spire-platform
description: SPIRE platform with integrated cert-manager, PostgreSQL, and SPIFFE workload identity
type: application

version: 0.1.0
appVersion: "1.0"

dependencies:
  - name: cert-manager
    version: v1.20.2
    repository: https://charts.jetstack.io
    condition: certManager.enabled
    alias: cert-manager